 #!/bin/bash

# Oppretter scratch org
sf org create scratch --definition-file config/project-scratch-def.json --alias $1 --duration-days $2 --set-default --json --wait 30

# Installer platform-data-model ver. 0.1.33.1
sf package install --package 04tQC0000017Qd7YAE --no-prompt --wait 30 --publish-wait 30

# Installer custom-metadata-dao ver. 0.1.33.1
sf package install --package 04tQC0000017QgLYAU --no-prompt --wait 30 --publish-wait 30

# Installer custom-permission-helper ver. 0.1.33.1
sf package install --package 04tQC0000017QejYAE --no-prompt --wait 30 --publish-wait 30

# Installer feature-toggle ver. 0.1.48
sf package install --package 04tQC000001C7jVYAS --no-prompt --wait 30 --publish-wait 30

# Installer crm-platform-base ver. 0.308
sf package install --package 04tQC000001UGhtYAG --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-thread-view ver. 0.8
sf package install --package 04tQC0000011athYAA --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-journal-utilities ver. 0.55
sf package install --package 04tQC0000012pVhYAI --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-platform-reporting ver. 0.47
sf package install --package 04tQC000001WtgjYAC --no-prompt --installation-key $3 --wait 30 --publish-wait 30



# Installer crm-shared-flowComponents ver. 0.4
sf package install --package 04t7U0000008qz4QAA --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-platform-access-control ver. 0.177
sf package install --package 04tQC000001AGR7YAO --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-community-base ver. 0.145
sf package install --package 04tQC000001MXeHYAW --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-henvendelse ver. 0.207
sf package install --package 04tQC000001TjKXYA0 --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-henvendelse-base ver. 0.38
sf package install --package 04tQC000001So7lYAC --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-platform-integration ver. 0.173
sf package install --package 04tQC000001Ms4PYAS --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-shared-user-notification ver. 0.27 
sf package install --package 04tQC0000012h6jYAA --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-platform-oppgave ver. 0.84
sf package install --package 04tQC000001UvwLYAS --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-hot-felles ver. 0.20
sf package install --package 04tQC000001Y1txYAC --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Dytt kildekoden til scratch org'en
sf project deploy start

# Tildel tilatelsessett til brukeren
sf org assign permset --name HOT_Lesehjelp_Admin

# Publish Experience Site
sf community publish --name lesehjelp

# Oppretter testdata
sf apex run --file scripts/apex/createTestData.apex

# done